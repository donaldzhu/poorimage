const MAX_SPAWN_COUNT = 50
const CLEAR_SPAWN_COUNT = 20
const IMAGE_AMOUNT = 65
const MIN_INTERVAL = 5000
const MAX_INTERVAL = 6500

const PROGRESSIVE_POSITION = {
  MAX_AMOUNT: 20,
  STARTING_POSITIONS: [
    [18, 42], // top
    [16, 60], // left
  ],
  ACCUMULATOR: [-0.75, 1.25] // min, max
}